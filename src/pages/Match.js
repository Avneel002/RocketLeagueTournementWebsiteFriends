import React from "react";

export default function Match({ setPage }) {
  return (
    <div>
      <h1>Match</h1>
      <p>niggas</p>
      <button onClick={() => setPage("dashboard")}>Back</button>
    </div>
  );
}
